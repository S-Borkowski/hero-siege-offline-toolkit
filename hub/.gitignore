# The root .gitignore bans *.png outright, for extracted game sprites. The hub's
# icon set is six generated squares regenerated by `npm run icons`, and Tauri
# will not bundle without them -- so they are re-included here, narrowly.
!src-tauri/icons/*.png

node_modules/
dist/
src-tauri/target/
src-tauri/gen/
